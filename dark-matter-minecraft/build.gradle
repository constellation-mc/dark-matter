dependencies {
    fabric(project, [
            "fabric-api-base",
            "fabric-resource-loader-v0"
    ])
}

loom {
    accessWidenerPath = file("${getRootProject().projectDir.absolutePath}/dark-matter-minecraft/src/main/resources/dark-matter.accesswidener")
}