pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        gradlePluginPortal()
    }
}

rootProject.name = "dark-matter"

include("${rootProject.name}-base")
include("${rootProject.name}-analytics")
include("${rootProject.name}-analytics-mixpanel")
include("${rootProject.name}-content")
include("${rootProject.name}-danger")
include("${rootProject.name}-enums")
include("${rootProject.name}-glitter")
include("${rootProject.name}-minecraft")
include("${rootProject.name}-mirage")
include("${rootProject.name}-recipe-book")